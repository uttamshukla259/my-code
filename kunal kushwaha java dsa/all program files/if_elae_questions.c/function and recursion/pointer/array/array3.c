#include <stdio.h>

int main()
{
    int a[] = {10, 20, 50};
    printf("the value of a 1 is %d\n ", a[0]);
    printf("the value of a 2 is %d\n ", a[1]);
    printf("the value of a 3 is %d\n ", a[2]);

    return 0;
}